%h2
  #{t('hello')}, #{@resource.name}!
%p= t('devise.mailer.unlock_instructions.p1')
%p= t('devise.mailer.unlock_instructions.p2')
%p= link_to t('devise.mailer.unlock_instructions.unlock_my_account'), unlock_url(@resource, unlock_token: @token)
%p
  #{t('devise.mailer.best_regards')},
  %br
  = t('devise.mailer.filmer_team')
