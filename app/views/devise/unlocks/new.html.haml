-title(t('users.resend_unlock_instructions'))
.flexbox.flex-center.container-margin-lg.flex-column
  .mdl-card.mdl-shadow--2dp
    = render('layouts/messages')
    .mdl-card__title.mdl-card--border
      .mdl-card__title-text= t('users.resend_unlock_instructions')
    = form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f|
      .mdl-textfield.mdl-js-textfield.mdl-textfield--floating-label.block-center
        = f.label :email, t('users.email'), :class => 'mdl-textfield__label'
        = f.email_field :email, :class => "mdl-textfield__input"
      .mdl-card__actions
        = f.submit t('send'), :class => 'mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect mdl-button--accent'
    .mdl-card__supporting-text
      = render "devise/shared/links"

